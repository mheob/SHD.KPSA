<UserControl x:Class="SHD.KPSA.Tools.Utils.SharedViews.FooterStart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:rx="clr-namespace:SHD.KPSA.Tools.Utils.Properties"
             mc:Ignorable="d"
             d:DesignHeight="80" d:DesignWidth="300">

    <!-- ReSharper disable Xaml.StaticResourceNotResolved -->
    <StackPanel Style="{StaticResource FooterButtonGroup}">
        <Button x:Name="ButtonStart"
                Content="{x:Static rx:Resources.ButtonStart}"
                Style="{StaticResource FooterButtonRight}"
                Command="{Binding StartGenerationCommand}"
                CommandParameter="{Binding ElementName=DataGridFiles, Path=ItemsSource, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
    <!-- ReSharper restore Xaml.StaticResourceNotResolved -->
</UserControl>