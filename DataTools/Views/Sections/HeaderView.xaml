<UserControl x:Class="DataTools.Views.Sections.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mvvm="http://prismlibrary.com/"
             xmlns:rx="clr-namespace:DataTools.Properties"
             xmlns:vm="clr-namespace:DataTools.ViewModels.Sections"
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:HeaderDataViewModel, IsDesignTimeCreatable=True}"
             d:DesignWidth="375">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Button x:Name="ButtonReverse" Grid.Column="0"
                Style="{StaticResource ButtonBackNav}"
                Command="{Binding NavigateBackCommand}"/>

        <TextBlock Grid.Column="1" Grid.Row="0"
                   Text="{Binding Title}"
                   Style="{StaticResource WindowHeading}"/>
    </Grid>
</UserControl>