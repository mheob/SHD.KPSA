<UserControl x:Class="SHD.KPSA.Tools.Changelog.Views.ChangelogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SHD.KPSA.Tools.Changelog.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ChangelogViewModel}">

    <Grid Margin="12">
        <ScrollViewer x:Name="ScrollViewerMainContent"
                      CanContentScroll="True"
                      Padding="8"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <DockPanel x:Name="StackPanelMainContent"
                       Margin="16,8">
                <Label x:Name="LabelContent" DockPanel.Dock="Top"
                       Content="{Binding BuildNote, FallbackValue=Changelog__Items}"
                       HorizontalContentAlignment="Right"
                       FontFamily="Consolas"
                       FontStyle="Italic" />

                <ItemsControl ItemsSource="{Binding ChangelogLines}" />
            </DockPanel>
        </ScrollViewer>
    </Grid>

</UserControl>